#include <cstdio>

#include "tests.h"

int main()
{
    return maintest ();

    /*
    Matrix<dd> a(5, 3);
    //printf ("%f\n", a[0][0]);
    a[3][2] = 10;
    vector<dd> line;
    line.push_back (12.3);
    line.push_back (-7);
    line.push_back (5);
    a[0] = a[4] = a[3] = line;
    a[0][0] = 11.5;

    Matrix<dd> b(3, 1);
    b[0][0] = 11;
    b[1][0] = 0.4;
    b[2][0] = 2;

    Matrix<dd> c = a * b;

    for (int j = 0; j < c.h (); j++){
        for (int i = 0; i < c[j].len (); i++)
            printf ("%f\t", c[j][i]);
        printf ("\n");
    }

    return 0;
    */
}
